Task3.
-----------------------------------------------------------------------------------------------------------------------------------

«Для каждой русской строки необходимо найти все английские строки такие, что индекс 
Петренко для русской строки равен индексу Петренко для английской строки (без учёта комментария)
 !!!плюс индекс комментария!!!.»

Я не совсем понял нужно вывести строки с одинаковым индексом и индекс комментария или indexRu==indexEng+IndexCom, поэтому сделал второй вариант.
Строки определяются абзацами.
Вывод результата происходит в текстовый файл, который сохраняется по пути, указанному после нажатия Calculate.
Сам индекс считается по формуле арефметической прогрессии Sn=(a1+an)n/2, an=длинна строки -0.5,a1=0.5,n=длинна строки,(0,5+длинна строки-05)*длинна строки/2 и домножаем на длинну строки(по условию),
таким образом у нас выходит (длинна строки^3)/2
------------------------------------------------------------------------------------------------------------------------------------
Task4
------------------------------------------------------------------------------------------------------------------------------------
файл битый, задание не открывается
------------------------------------------------------------------------------------------------------------------------------------